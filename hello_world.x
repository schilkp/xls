
fn hello_xls(hello_string: u8[11]) {
    // Trace the value to stderr as output.
    let a = (s32:10);
    let b : (s32) = (s32:10,);
    trace!(hello_string);
    trace!(a);
    trace!(b);
}


#[test]
fn hello_test() {
  hello_xls("Hello, XLS!")
}

